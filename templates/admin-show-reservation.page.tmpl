{{template "admin" .}}

{{define "page-title"}}
    Reservation
{{end}}

{{define "content"}}
    {{ $res := index .Data "reservations" }}
    <div class="col-md-12">
        Show Reservation #{{ $res.FirstName }} {{ $res.LastName }}
    </div>
{{end}}